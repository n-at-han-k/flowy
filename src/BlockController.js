import { Controller } from "@hotwired/stimulus"

export default class BlockController extends Controller {
  static targets = []
  static values = {}

  connect() {
    //this.element.innerHTML = 'it works'
  }
}
